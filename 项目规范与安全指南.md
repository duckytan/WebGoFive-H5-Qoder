# 项目规范与安全指南

## 📋 概述

本文档规定了H5五子棋项目的编码规范、安全要求和质量标准，确保项目的可维护性和安全性。

---

## 🔒 安全性要求

### 1. 输入验证

#### 1.1 用户输入验证
```javascript
// ✅ 坐标输入验证
function validateCoordinates(x, y) {
    // 类型检查
    if (typeof x !== 'number' || typeof y !== 'number') {
        throw new Error('坐标必须是数字类型');
    }
    
    // 范围检查
    if (!Number.isInteger(x) || !Number.isInteger(y)) {
        throw new Error('坐标必须是整数');
    }
    
    // 边界检查
    if (x < 0 || x >= 15 || y < 0 || y >= 15) {
        throw new Error('坐标超出棋盘范围');
    }
    
    return true;
}

// ✅ 玩家参数验证
function validatePlayer(player) {
    if (![1, 2].includes(player)) {
        throw new Error('玩家参数必须是1或2');
    }
    return true;
}
```

#### 1.2 文件输入验证
```javascript
// ✅ 游戏文件加载验证
function validateGameFile(fileContent) {
    try {
        const data = JSON.parse(fileContent);
        
        // 必需字段检查
        const requiredFields = ['version', 'gameState', 'moves', 'board'];
        for (const field of requiredFields) {
            if (!(field in data)) {
                throw new Error(`缺少必需字段: ${field}`);
            }
        }
        
        // 版本兼容性检查
        if (!isCompatibleVersion(data.version)) {
            throw new Error(`不兼容的文件版本: ${data.version}`);
        }
        
        // 数据完整性检查
        if (!validateBoardData(data.board)) {
            throw new Error('棋盘数据无效');
        }
        
        return data;
    } catch (error) {
        throw new Error(`文件格式无效: ${error.message}`);
    }
}
```

### 2. XSS防护

#### 2.1 HTML内容过滤
```javascript
// ✅ HTML内容清理
function sanitizeHTML(input) {
    const div = document.createElement('div');
    div.textContent = input;
    return div.innerHTML;
}

// ✅ 安全的DOM操作
function setTextContent(element, text) {
    // 使用textContent而不是innerHTML
    element.textContent = text;
}
```

#### 2.2 动态内容处理
```javascript
// ✅ 安全的用户名显示
function displayPlayerName(name) {
    // 限制长度
    if (name.length > 20) {
        name = name.substring(0, 20) + '...';
    }
    
    // 清理特殊字符
    name = name.replace(/[<>\"'&]/g, '');
    
    return name;
}
```

### 3. 数据保护

#### 3.1 本地存储安全
```javascript
// ✅ 安全的本地存储
class SecureStorage {
    static PREFIX = 'gomoku_';
    static VERSION = '1.0';
    
    static setItem(key, value) {
        try {
            const data = {
                version: this.VERSION,
                timestamp: Date.now(),
                value: value
            };
            
            const serialized = JSON.stringify(data);
            localStorage.setItem(this.PREFIX + key, serialized);
            return true;
        } catch (error) {
            console.error('存储失败:', error);
            return false;
        }
    }
    
    static getItem(key) {
        try {
            const stored = localStorage.getItem(this.PREFIX + key);
            if (!stored) return null;
            
            const data = JSON.parse(stored);
            
            // 版本检查
            if (data.version !== this.VERSION) {
                this.removeItem(key);
                return null;
            }
            
            // 过期检查（30天）
            const maxAge = 30 * 24 * 60 * 60 * 1000;
            if (Date.now() - data.timestamp > maxAge) {
                this.removeItem(key);
                return null;
            }
            
            return data.value;
        } catch (error) {
            console.error('读取失败:', error);
            return null;
        }
    }
    
    static removeItem(key) {
        localStorage.removeItem(this.PREFIX + key);
    }
    
    static clear() {
        const keys = Object.keys(localStorage);
        keys.forEach(key => {
            if (key.startsWith(this.PREFIX)) {
                localStorage.removeItem(key);
            }
        });
    }
}
```

#### 3.2 敏感信息处理
```javascript
// ✅ 游戏统计数据保护
class GameStats {
    constructor() {
        this.stats = this.loadStats();
    }
    
    loadStats() {
        const data = SecureStorage.getItem('stats');
        return data || {
            gamesPlayed: 0,
            wins: 0,
            losses: 0,
            draws: 0
        };
    }
    
    updateStats(result) {
        // 验证结果参数
        if (!['win', 'loss', 'draw'].includes(result)) {
            throw new Error('无效的游戏结果');
        }
        
        this.stats.gamesPlayed++;
        this.stats[result === 'win' ? 'wins' : result === 'loss' ? 'losses' : 'draws']++;
        
        // 限制统计数据范围，防止数据篡改
        Object.keys(this.stats).forEach(key => {
            if (this.stats[key] < 0 || this.stats[key] > 999999) {
                this.stats[key] = 0;
            }
        });
        
        SecureStorage.setItem('stats', this.stats);
    }
}
```

---

## 📝 编码规范

### 1. JavaScript规范

#### 1.1 命名规范
```javascript
// ✅ 类名：PascalCase
class GameManager {}
class CanvasRenderer {}

// ✅ 方法名和变量名：camelCase
function makeMove() {}
let currentPlayer = 1;

// ✅ 常量：UPPER_SNAKE_CASE
const BOARD_SIZE = 15;
const MAX_PLAYERS = 2;

// ✅ 私有方法：下划线前缀
class Board {
    _validatePosition(x, y) {}
    _updateInternalState() {}
}

// ✅ 事件处理器：on + 动作
function onCanvasClick() {}
function onGameStateChange() {}
```

#### 1.2 注释规范
```javascript
/**
 * 五子棋游戏管理器
 * 负责协调所有组件，管理游戏流程
 * 
 * @class GameManager
 * @example
 * const game = new GameManager();
 * game.startNewGame();
 */
class GameManager {
    /**
     * 执行落子操作
     * 
     * @param {number} x - X坐标 (0-14)
     * @param {number} y - Y坐标 (0-14)
     * @returns {boolean} 是否成功落子
     * @throws {Error} 当坐标无效时抛出错误
     * 
     * @example
     * const success = gameManager.makeMove(7, 7);
     * if (success) {
     *     console.log('落子成功');
     * }
     */
    makeMove(x, y) {
        // 参数验证
        this._validateCoordinates(x, y);
        
        // 检查游戏状态
        if (this.gameState.status !== 'playing') {
            throw new Error('游戏未在进行中');
        }
        
        // 执行落子逻辑
        // ...
    }
    
    /**
     * 验证坐标有效性（私有方法）
     * @private
     */
    _validateCoordinates(x, y) {
        // 验证逻辑
    }
}
```

#### 1.3 错误处理规范
```javascript
// ✅ 统一的错误类型
class GameError extends Error {
    constructor(message, code, context) {
        super(message);
        this.name = 'GameError';
        this.code = code;
        this.context = context;
        this.timestamp = new Date().toISOString();
    }
}

// ✅ 错误代码常量
const ErrorCodes = {
    INVALID_COORDINATES: 'E001',
    POSITION_OCCUPIED: 'E002',
    FORBIDDEN_MOVE: 'E003',
    GAME_ENDED: 'E004',
    AI_ERROR: 'E005'
};

// ✅ 统一错误处理
function handleGameError(error, context) {
    // 记录错误
    console.error(`[${context}] ${error.message}`, {
        code: error.code,
        timestamp: error.timestamp,
        stack: error.stack
    });
    
    // 用户友好的错误提示
    const userMessage = getUserFriendlyMessage(error.code);
    showErrorMessage(userMessage);
    
    // 错误恢复
    if (isRecoverableError(error)) {
        attemptErrorRecovery(error);
    }
}
```

### 2. CSS规范

#### 2.1 选择器规范
```css
/* ✅ BEM命名约定 */
.game-board {}
.game-board__cell {}
.game-board__cell--active {}
.game-board__cell--forbidden {}

/* ✅ 组件样式分组 */
/* === 游戏棋盘组件 === */
.game-board {
    position: relative;
    background: #d4a574;
    border: 2px solid #8b4513;
}

.game-board__grid {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    pointer-events: none;
}

/* === 控制面板组件 === */
.control-panel {
    display: flex;
    flex-direction: column;
    gap: 10px;
    padding: 20px;
}
```

#### 2.2 响应式设计规范
```css
/* ✅ 移动优先设计 */
.game-container {
    /* 移动端样式 */
    padding: 10px;
    flex-direction: column;
}

/* 平板设备 */
@media (min-width: 768px) {
    .game-container {
        padding: 20px;
        flex-direction: row;
    }
}

/* 桌面设备 */
@media (min-width: 1024px) {
    .game-container {
        max-width: 1200px;
        margin: 0 auto;
    }
}
```

### 3. HTML规范

#### 3.1 语义化标签
```html
<!-- ✅ 语义化结构 -->
<main class="game-main">
    <header class="game-header">
        <h1>H5五子棋</h1>
        <nav class="game-nav">
            <button type="button" class="btn btn--primary" id="new-game">
                新游戏
            </button>
        </nav>
    </header>
    
    <section class="game-board-section">
        <canvas 
            id="game-canvas" 
            width="480" 
            height="480"
            aria-label="五子棋棋盘"
            role="grid">
        </canvas>
    </section>
    
    <aside class="game-info">
        <div class="player-info" aria-live="polite">
            <span id="current-player">当前玩家：黑棋</span>
        </div>
    </aside>
</main>
```

#### 3.2 无障碍性规范
```html
<!-- ✅ 无障碍支持 -->
<button 
    type="button"
    class="btn btn--undo"
    id="undo-btn"
    aria-label="悔棋"
    title="撤销最后一步棋"
    disabled>
    悔棋
</button>

<div 
    class="game-status" 
    role="status" 
    aria-live="assertive"
    aria-atomic="true">
    <span id="status-text">游戏进行中</span>
</div>

<!-- 键盘导航支持 -->
<div class="game-controls" role="toolbar" aria-label="游戏控制">
    <button type="button" tabindex="0" accesskey="n">新游戏 (Alt+N)</button>
    <button type="button" tabindex="0" accesskey="u">悔棋 (Alt+U)</button>
</div>
```

---

## 🔍 代码质量标准

### 1. 性能要求

#### 1.1 渲染性能
```javascript
// ✅ 高效的Canvas渲染
class CanvasRenderer {
    constructor(canvasId) {
        this.canvas = document.getElementById(canvasId);
        this.ctx = this.canvas.getContext('2d');
        
        // 性能优化设置
        this.ctx.imageSmoothingEnabled = false; // 关闭抗锯齿
        this.canvas.style.imageRendering = 'pixelated'; // 像素化渲染
        
        // 缓存常用对象
        this.imageCache = new Map();
        this.pathCache = new Map();
    }
    
    render(board, gameState, options = {}) {
        // 性能监控
        const startTime = performance.now();
        
        try {
            // 只重绘变化的区域
            this.clearDirtyRegions();
            this.renderBoard();
            this.renderPieces(board);
            this.renderUI(gameState);
            
        } finally {
            // 记录渲染时间
            const renderTime = performance.now() - startTime;
            if (renderTime > 16) { // 超过60fps阈值
                console.warn(`渲染耗时过长: ${renderTime.toFixed(2)}ms`);
            }
        }
    }
}
```

#### 1.2 内存管理
```javascript
// ✅ 内存泄漏预防
class ResourceManager {
    constructor() {
        this.resources = new Set();
        this.timers = new Set();
        this.listeners = new Set();
    }
    
    addTimer(timerId) {
        this.timers.add(timerId);
    }
    
    addEventListener(element, event, handler) {
        element.addEventListener(event, handler);
        this.listeners.add({ element, event, handler });
    }
    
    cleanup() {
        // 清理定时器
        this.timers.forEach(timerId => clearTimeout(timerId));
        this.timers.clear();
        
        // 移除事件监听器
        this.listeners.forEach(({ element, event, handler }) => {
            element.removeEventListener(event, handler);
        });
        this.listeners.clear();
        
        // 清理其他资源
        this.resources.clear();
    }
}
```

### 2. 测试覆盖率要求

#### 2.1 测试覆盖率目标
```
核心模块测试覆盖率要求：
- Board类：≥ 95%
- RuleEngine类：≥ 90%
- GameManager类：≥ 85%
- CanvasRenderer类：≥ 80%
- 其他工具类：≥ 70%

总体测试覆盖率目标：≥ 85%
```

#### 2.2 测试质量检查
```javascript
// ✅ 测试质量验证
class TestQualityChecker {
    static checkTestCoverage(testResults) {
        const coverage = this.calculateCoverage(testResults);
        
        const requirements = {
            'Board': 95,
            'RuleEngine': 90,
            'GameManager': 85,
            'CanvasRenderer': 80
        };
        
        const report = {};
        Object.entries(requirements).forEach(([module, required]) => {
            const actual = coverage[module] || 0;
            report[module] = {
                required: required,
                actual: actual,
                passed: actual >= required
            };
        });
        
        return report;
    }
    
    static generateCoverageReport(report) {
        console.log('测试覆盖率报告:');
        Object.entries(report).forEach(([module, data]) => {
            const status = data.passed ? '✅' : '❌';
            console.log(`${status} ${module}: ${data.actual}% (要求: ${data.required}%)`);
        });
    }
}
```

---

## 📊 质量检查清单

### 1. 代码提交前检查
```bash
# 自动化检查脚本
#!/bin/bash

echo "开始代码质量检查..."

# 1. ESLint检查
echo "1. 运行ESLint..."
npx eslint js/*.js
if [ $? -ne 0 ]; then
    echo "❌ ESLint检查失败"
    exit 1
fi

# 2. 运行测试
echo "2. 运行测试套件..."
# 打开test/test-runner.html进行测试

# 3. 性能基准测试
echo "3. 性能基准测试..."
# 运行性能测试

# 4. 安全性检查
echo "4. 安全性检查..."
# 检查敏感信息泄露
grep -r "password\|secret\|key" js/ && echo "❌ 发现敏感信息" && exit 1

echo "✅ 所有检查通过"
```

### 2. 发布前验收清单
```
功能验收:
☐ 基础游戏功能正常
☐ 禁手规则正确实现
☐ AI对战功能稳定
☐ 所有交互功能正常
☐ 错误处理完善

性能验收:
☐ 页面加载 < 3秒
☐ 操作响应 < 100ms
☐ AI计算 < 3秒
☐ 内存使用 < 50MB
☐ 渲染帧率 ≥ 60fps

兼容性验收:
☐ Chrome 60+ 正常
☐ Firefox 55+ 正常
☐ Safari 12+ 正常
☐ Edge 79+ 正常
☐ 移动设备适配良好

安全性验收:
☐ 输入验证完整
☐ XSS防护有效
☐ 数据存储安全
☐ 错误信息不泄露敏感信息
☐ 第三方依赖安全
```

---

## 🚨 应急响应计划

### 1. 严重bug处理流程
```
1. 立即评估影响范围
2. 创建hotfix分支
3. 快速修复问题
4. 运行关键测试
5. 部署修复版本
6. 监控修复效果
7. 更新文档和日志
```

### 2. 性能问题处理
```javascript
// ✅ 性能问题诊断工具
class PerformanceDiagnostic {
    static diagnoseRenderingIssues() {
        const canvas = document.getElementById('game-canvas');
        const ctx = canvas.getContext('2d');
        
        // 检测渲染性能
        const startTime = performance.now();
        
        // 模拟重渲染
        for (let i = 0; i < 100; i++) {
            ctx.clearRect(0, 0, canvas.width, canvas.height);
            // 绘制棋盘
        }
        
        const avgTime = (performance.now() - startTime) / 100;
        
        if (avgTime > 16) {
            console.warn('渲染性能不佳:', {
                averageTime: avgTime,
                recommendation: '考虑优化绘制逻辑或减少重绘区域'
            });
        }
        
        return avgTime;
    }
    
    static checkMemoryLeaks() {
        if (performance.memory) {
            const memory = performance.memory;
            const usedMB = memory.usedJSHeapSize / 1024 / 1024;
            
            if (usedMB > 50) {
                console.warn('内存使用过高:', {
                    used: usedMB.toFixed(2) + 'MB',
                    recommendation: '检查是否存在内存泄漏'
                });
            }
            
            return usedMB;
        }
        
        return null;
    }
}
```

---

*本规范文档确保项目开发过程中的代码质量、安全性和可维护性，是所有开发者必须遵循的标准。*